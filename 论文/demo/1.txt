要做的事：1、刷题：算法题，sql题 
	2、算法知识准备  面试问题收集，项目梳理（重要）
	3、中期准备
5月份的计划顺序：
	1、上午，sql 8.5-9.5 其余时间算法   两个月能提高多少？
	2、补项目代码，比赛等，提升挖掘和分析能力
	3、论文准备：框架重构，开始写
                

核心能力：数据挖掘，sql，数据结构和算法

数据清洗-->baseline -->继续构建特征：查找特征相关性分析，决策树查找特征重要性 ，重要特征和与其相关的特征进行特征衍生

路线是什么呢？
把计算标签改成语义相似度，所以target是语义相似度得分，
可以把TDA中的量词作为评价标准，去计算他们tda中所有词的语义特征，在计算相似度，得到target
target有五个维度，这里就是五个相似度得分

几个问题：1、训练计算语义相似度
                2、数据：最后的标签是加权和，并不是每一条语句的相似度，要取一个平均值，
                3、模型构建和训练，
                     语料标签 = 各个维度的相似度，然后求加权二次训练
                     主要是加权，这里怎么能连接起来，想法：分别计算语料数量，线性层乘以他的系数，最后再相加？
                     那增量训练怎么做？
                     用一次训练得到的相似度，再加上增量数据（少一些，减少干扰），得到人的标签
                4、其他对比实验：不使用语义相似度计算对比，不使用增量数据计算对比，两者都不使用对比





             